export interface ProductPostModel {
    name: string;
    description: string;
    photoUrl: string;
    price: number;
}

export interface ProductModel {
    id: string;
    name: string;
    description: string;
    photoUrl: string;
    price: number;
}
